<?php

$URL = $_SERVER['HTTP_HOST'];

// $SALT = hash('sha256', 'change this salt!');


// Create database if it doesn't exist
// $DB = new SQLite3($_SERVER['DOCUMENT_ROOT'] . '/db/users.db');
// $DB->exec("CREATE TABLE IF NOT EXISTS users (id INTEGER PRIMARY KEY, name TEXT, email TEXT, directory TEXT UNIQUE, passwordHash TEXT)"); 

?>